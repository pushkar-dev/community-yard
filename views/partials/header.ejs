<!-- ... (your existing code above) ... -->

<body>
    <!-- Sidebar Start -->
    <div class="position-relative bg-white d--inline-flex p-1">
        <!-- Spinner Start -->
        <div id="spinner"
            class="show bg-white position-fixed translate-middle w-100 vh-100 top-50 start-50 d-flex align-items-center justify-content-center">
            <div class="spinner-border text-primary" style="width: 3rem; height: 3rem;" role="status">
                <span class="sr-only">Loading...</span>
            </div>
        </div>
        <!-- Spinner End -->
        <div class="sidebar pe-4 pb-3">
            <!-- ... (your existing code for sidebar) ... -->
        </div>
    </div>
    <!-- Sidebar End -->

    <!-- Content Start -->
    <div class="content">
        <!-- Navbar Start -->
        <nav class="navbar navbar-expand bg-light navbar-light sticky-top px-0 py-0">
            <a href="/" class="navbar-brand d-flex d-lg-none me-4">
                <h2 class="text-primary mb-0"><i class="fa fa-icon-name"></i></h2> <!-- Specify the Font Awesome icon class -->
            </a>
            <a href="#" class="sidebar-toggler flex-shrink-0">
                <i class="fa fa-bars"></i>
            </a>
            <form class="d-none d-md-flex ms-4">
                <!--input class="form-control border-0" type="search" placeholder="Search"-->
            </form>
            <div class="navbar-nav align-items-center ms-auto">
                <div class="nav-item dropdown">
                    <!-- ... (your existing message dropdown) ... -->
                </div>
                <div class="nav-item dropdown">
                    <!-- ... (your existing notifications dropdown) ... -->
                </div>
                <div class="nav-item dropdown">
                    <% if(user !== null) { %>
                    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                        <img class="rounded-circle me-lg-2" src="<%= user.pic %>" alt=""
                            style="width: 40px; height: 40px;">
                        <span class="d-none d-lg-inline-flex"><%= user.name %> </span>
                    </a>
                    <div
                        class="dropdown-menu dropdown-menu-end bg-light border-0 rounded-0 rounded-bottom m-0">
                        <a href="/logout" class="dropdown-item">Log Out</a>
                    </div>
                    <% } else { %>
                    <a href="auth/google" class="nav-link">
                        <span class="d-none d-lg-inline-flex">Sign-in</span>
                    </a>
                    <% } %>
                </div>
            </div>
        </nav>
        <!-- Navbar End -->

        <!-- Your existing content goes here -->
        <!-- ... -->

    </div>
    <!-- Content End -->

    <!-- ... (your existing script imports) ... -->

</body>

</html>
